using UnityEngine;
using System.Collections;

public class dink : MonoBehaviour {

	// Use this for initialization
	void Start () {
        audio.pitch = Random.Range(0.9f, 1.1f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
